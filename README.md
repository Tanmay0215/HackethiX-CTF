# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react/README.md) uses [Babel](https://babeljs.io/) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh

## Solutions / Flags

Here are the flags hidden throughout the application:

1.  `CTF{cookie_monster}` - Found in browser cookies.
2.  `CTF{c0ns0l3_l0g}` - Found in the browser console logs.
3.  `CTF{cl1ck3r_g4m3_pr0}` - Found in a toast notification (requires clicking the secret button).
4.  `ctf{h1dd3n_1n_pl41n_s1ght}` - Found in the API response/JSON data on the Flag page.
5.  `CTF{sql_b4s1cs_101}` - Found by simulating an SQL injection on the Login page (admin/admin).
6.  `CTF{3rr0r_p4g3}` - Found on the 404 Not Found page.
